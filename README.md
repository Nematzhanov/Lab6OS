# 🧠 Lab6OS — Семафоры и разделяемая память в Linux

## 📘 Описание проекта

Этот проект демонстрирует использование **семафоров** и **разделяемой памяти**  
в операционной системе Linux для синхронизации работы **родительского и дочернего процессов**.

Цель программы — показать, как два процесса могут совместно работать с одним и тем же массивом чисел,  
избегая конфликтов при помощи механизмов синхронизации.

---

## 🧩 Что такое семафор

**Семафор** — это механизм синхронизации, который контролирует доступ к общим ресурсам.  
Он работает как «светофор» 🚦 — пропускает процесс, если ресурс свободен,  
и заставляет ждать, если он занят.

🔹 Когда процесс входит в критическую секцию — семафор блокируется (операция **wait**).  
🔹 Когда процесс выходит — семафор освобождается (операция **signal**).

> 💬 Это позволяет избежать одновременного доступа и “гонок данных” (race conditions).

В Linux для работы с семафорами используются функции:
```cpp
semget(), semop(), semctl()
````

---

## ⚙️ Как работает программа

1. Родительский процесс создаёт **разделяемую память** и **массив случайных чисел**.
2. Для каждого элемента массива создаётся **отдельный семафор**.
3. Затем вызывается `fork()`, создающий **дочерний процесс**.
4. Дочерний процесс выполняет **сортировку пузырьком**, блокируя элементы семафорами.
5. Родительский процесс параллельно читает массив, также используя семафоры.
6. После завершения сортировки память и семафоры освобождаются.

---

## 🧠 Зачем нужны семафоры

Без семафоров два процесса могли бы одновременно изменить один и тот же элемент массива,
что привело бы к ошибкам в сортировке.
Семафоры решают эту проблему, гарантируя, что каждый элемент массива
обрабатывается **только одним процессом** в данный момент.

---

## 🧪 Пример работы программы

```
Введите N, Min, Max: 5 10 50
Исходный массив:
  arr[0] = 25
  arr[1] = 42
  arr[2] = 13
  arr[3] = 38
  arr[4] = 19

=== Дочерний процесс начал сортировку ===
...
Отсортированный массив:
13 19 25 38 42
Память и семафоры освобождены.
```

---

## ⚙️ Сборка и запуск

### 🔧 1. Сборка через CMake

```bash
mkdir build
cd build
cmake ..
make
```

### ▶️ 2. Запуск программы

```bash
./Lab6OS
```

---

## 🧪 Автоматическое тестирование (CTest)

Для проверки корректности программы используется **CTest**.
Тест подаёт входные данные `5 10 50` и проверяет наличие строки
**«Отсортированный массив»** в выводе.

### Запуск тестов:

```bash
ctest -V
```

Результат:

```
1/1 Test #1: Lab6_Test ............   Passed   0.95 sec
100% tests passed, 0 tests failed out of 1
```

---

## 🧰 Bash-скрипт для автоматизации

Чтобы не вводить команды вручную, можно использовать скрипт **run_tests.sh**:

```bash
#!/bin/bash
echo "🧩 Сборка проекта..."
rm -rf build
mkdir build && cd build
cmake ..
make -j$(nproc)
echo "🧪 Запуск тестов..."
ctest -V
echo "✅ Готово!"
```

Запуск:

```bash
./run_tests.sh
```

---

## 📂 Структура проекта

```
Lab6OS/
├── Code/
│   └── main.cpp          # Исходный код программы
├── CMakeLists.txt        # Настройки сборки
├── run_tests.sh          # Скрипт для сборки и тестов
├── .gitignore            # Игнор временных файлов
└── README.md             # Документация проекта
```

---

## ✍️ Автор

**Нематжанов Мухаммадзахид**
📚 Группа: *4093*
🧑‍💻 Предмет: *Операционные системы*
📅 Тема: *Семафоры в LINUX как средство синхронизации процессов*

---

> 🟢 Проект создан для демонстрации взаимодействия процессов в Linux
> с использованием системных вызовов `fork`, `shmget`, `semget`, `semop`, `wait` и `shmdt`.

---
Nematzhanov Mukhammadzakhid<br/>
[![Telegram](https://img.shields.io/badge/Telegram-2CA5E0?logo=telegram&logoColor=white)](https://t.me/nematzhonov)
[![Instagram](https://img.shields.io/badge/Instagram-E4405F?logo=instagram&logoColor=white)](https://www.instagram.com/nematzhanov_?igsh=NTc4MTIwNjQ2YQ==)
[![VK](https://img.shields.io/badge/VK-0077FF?logo=vk&logoColor=white)](https://vk.com/nematzhanov_m)
